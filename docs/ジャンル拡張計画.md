# ジャンル拡張計画（ハッカソン → ビジコン・展示会・アクセラ・共創・補助金）

| 項目 | 内容 |
| --- | --- |
| 版 | 0.1.0（2026-09-22 起票） |
| 状態 | 計画。着手前 |
| 関連 | ADR-008（テーマ単位収集）、ADR-010（プール探索）、ADR-006（主催者投稿）、ADR-009（計測）、要件定義書 §4.2 |

## なぜ広げるのか

ハッカソンに絞って形は整った（テーマ単位のバッチ収集 → 共有プール → プール探索
エージェント）。同じ仕組みは「締切があり、公開Webで告知され、見逃すと終わる」
機会すべてに効く。ユーザー（エンジニア、起業準備者）にとって、ハッカソンと
ビジコンとアクセラは**同じ判断軸で並ぶべきもの**である。

## 拡張しても変えないこと

- 申込締切と実施日を分けて出す（上位§5-1）
- 根拠のない値を採用しない。年・締切・日付を推測しない（§6.5）
- 収集はテーマ単位のバッチ、ユーザー向けは検索なしのプール探索（ADR-008 / ADR-010）
- 主催者投稿と計測の仕組み（ADR-006 / ADR-009）

## ベクトル化（Embedding）を今は入れない理由

ジャンルは**属性で切れる**。「ビジコンだけ」「大阪の補助金」は `kind` と地域の
決定論フィルタで足り、ベクトルの出番はない。ベクトルが効くのは同じジャンルの中で
「ヘルスケア領域のアクセラ」「製造業向けの共創」のようにテーマの言い換えが要る
ときで、それはプールが数百件・ジャンル内で絞っても数十件残る規模になってからである。

コストの面でも動機は薄い。「探す」1 回は Gemini 2 呼び出しで ≒0.6 円、費用の主は
Grounding 検索（バッチ側に隔離済み）で、embedding はそこに触れない。

**再検討の条件**: 1 ジャンル内のプールが 50 件を超え、語の一致では上位 20 件の
足切りが実害を出すようになったとき。そのときは「収集時に埋め込みを保存 →
決定論スコアとコサインで全件を並べ、モデルは上位 5 件の理由だけ」を足す。

## 本当の課題は「構造が違う」こと

現在の `Event` は `eventStart` 必須で、「申込締切 → 開催日」の 2 軸を前提にしている。

| ジャンル | 締切 | 実施 | 固有の属性 | 2軸に乗るか |
| --- | --- | --- | --- | --- |
| ハッカソン | 申込締切 | 開催日 | テーマ、形式 | ○ |
| ビジコン | 応募締切 | 一次審査 / 最終審査・発表日 | 賞金、対象（学生/法人）、応募形式 | ○（実施 = 最終審査日） |
| アクセラ | 募集締切 | プログラム期間、Demo Day | 出資の有無、期間、対象ステージ | △（実施が期間） |
| 共創・オープンイノベーション | 公募締切 | 採択後の協業期間 | 募集企業、募集テーマ | △（実施が期間） |
| 展示会 | 出展申込締切 / 来場登録締切 | 会期 | 出展料、出展 or 来場 | △（締切が 2 種類） |
| 補助金 | 公募締切 | **無い**（事業実施期間） | 補助率、上限額、対象者、対象地域 | ✗ |

## 段階と順序

構造がハッカソンに近い順に広げる。各段階は独立してデプロイでき、前の段階を壊さない。

### 段階1: `kind` を導入する（ビジコンを同時に載せる）

> 事前調査（下記）の結果、`eventStart` の任意化・ラベル表のジャンル別化・`milestones` を
> 段階1 に前倒しした。抽出の仕組み自体は変えなくてよい（行ラベルが効かないのは
> ジャンル差ではなく実ページ全般の性質で、引用抽出が既に引き受けている）。

- 契約 `event.json`: `kind`（`hackathon | contest | accelerator | cocreation | exhibition | subsidy`）を追加。既存の `category` は表示用の細分として残すが、絞り込みは `kind` で行う
- **`eventStart` を任意にする**（`kind != "hackathon"` で実施日が無い告知がある）。2 軸表示は実施日が無ければ「締切のみ」を出す
- **`dates.milestones`**（`{label, at, precision}` 最大 5）を足し、エントリー開始 / 一次選考発表 / 最終審査会 / 結果発表 を落とさずに持つ
- **ラベル表を `kind` ごとに**持つ（`workflows/themes.py` の各テーマに `deadline_labels` / `exclude_labels` / `milestone_labels`）。「提出締切」はハッカソンでは除外、ビジコンでは採用
- 締切のラベル行が範囲（「募集期間 A〜B締切」）のときは `parse_range` の **end** を締切にする
- 抽出: `extractor._CATEGORY_WORDS` を `kind` 判定に拡張（`ビジネスコンテスト`/`ビジコン`/`アイデアコンテスト` → contest）。ラベル表 `APPLICATION_LABELS` に `応募締切`（既にある）、`エントリー締切`（既にある）を確認し、`一次審査` `最終審査` `結果発表` を `EVENT_DATE_LABELS` とは別の「マイルストーン」として扱うかを決める（段階2）
- テーマ: `kind × 地域` に一般化。まず `contest-kansai` / `contest-kanto` / `contest-online` を足す（7 テーマ × 4 クエリ = 28 検索/日、`DAILY_GROUNDING_CAP` を 40 → 60）
- プール探索: 解釈に `kinds`（配列）を足し、「ビジコン」「コンテスト」→ contest。絞り込みは決定論
- UI: チップに ジャンル（すべて / ハッカソン / ビジコン）を足す。行に `kind` のラベル
- 評価: `evals/cases/12-contest-*.json` を 5 件（応募締切と審査日、賞金の記述、学生限定）

### 段階2: 実施が「期間」のジャンル（アクセラ・共創）

- `milestones` は段階1 で入る。ここでは「Demo Day」「採択発表」「プログラム期間」の語彙を足す
- `attributes`（`Record<string, string>`、最大 10、各 60 字）を足し、ジャンル固有の値（賞金、出資、対象ステージ、募集企業）を表示と絞り込みに使う。**構造化はしない**（ジャンルごとにスキーマを分けると契約が増えすぎる）
- 抽出: `milestones` は既存の `find_labelled` + `parse_date` で拾える。ラベル表をジャンル別に持つ（`workflows/themes.py` の各テーマに `deadline_labels` / `milestone_labels` を持たせる）
- プール探索: 「締切が近い順」だけでなく「実施が近い順」で並べられるようにする

### 段階3: 展示会（締切が 2 種類）

- `dates.applicationDeadline` は**出展申込締切**、来場登録の締切は `milestones` に `来場登録締切` として持つ
- `attributes` に `出展料` `来場無料` `主催者`。UI は「出展したい / 見に行きたい」でフィルタ（`attributes` の有無で決定論的に）
- 会期は `eventStart` 〜 `eventEnd` にそのまま乗る

### 段階4: 補助金（別モデル）

- **Web 収集ではなく jGrants の公開 API から取る**（デジタル庁、構造化データ）。スクレイピングと LLM 抽出が不要になり、精度が桁違いに上がる。`clients/jgrants.py` を足し、テーマ `subsidy-*` はこのクライアントを使う（Grounding 検索は使わない = 検索代ゼロ）
- 契約: `eventStart` の任意化は段階1 で済んでいる。補助金は常に実施日なしで、代わりに `attributes` に `事業実施期間` `補助率` `上限額` `対象者` `対象地域`
- 2 軸表示は「公募締切 → 事業実施期間」。実施日が無いので「開催日」ラベルを `kind` で切り替える
- 影響が大きいので ADR を 1 本書く（ADR-011 想定）

## 段階ごとの影響範囲（見積もり）

| 段階 | 契約 | 抽出 | テーマ | UI | 評価 |
| --- | --- | --- | --- | --- | --- |
| 1 | `kind`, `eventStart` 任意化, `milestones` | `kind` 判定、ラベル表のジャンル別化、範囲締切 | +3 | チップ、行ラベル、実施日なしの表示、時系列 | +8 ケース |
| 2 | `attributes` | アクセラ・共創の語彙 | +6 | 属性表示 | +6 ケース |
| 3 | — | 出展/来場の締切分け | +3 | 出展/来場フィルタ | +4 ケース |
| 4 | — | jGrants クライアント | +4（検索なし） | 補助金の日付表示 | +5 ケース |

## コスト見積もり（Grounding 検索）

| 構成 | テーマ数 | 検索/日 | 月 | 概算 |
| --- | --- | --- | --- | --- |
| 現在（ハッカソン 4 地域） | 4 | 16 | 480 | 無料枠内 |
| 段階1（+ビジコン 3） | 7 | 28 | 840 | 無料枠内 |
| 段階2（+アクセラ・共創 6） | 13 | 52 | 1,560 | 無料枠内 |
| 段階3（+展示会 3） | 16 | 64 | 1,920 | 無料枠内 |
| 段階4（+補助金 4、API 経由で検索なし） | 20 | 64 | 1,920 | 無料枠内（月 5,000 件） |

`DAILY_GROUNDING_CAP` は段階ごとに 40 → 60 → 90 → 110 と上げる。既知ページの省略が
効き始めれば実際の消費はこれより減る。

## 事前調査: アクセラ・共創の告知ページを読む（2026-09-22 実施）

段階2 でも、実ページを現在のパイプラインに通してから作った。対象は AUBA（eiicon）と
Creww Growth。この 2 サイトは指名で見に行ってよい、というユーザーの判断がある。

### 分かったこと

1. **詳細ページは取れる。ただしホストを間違えると 403**。`auba-edge.eiicon.net`（画像と
   配信用）は 403 を返すが、`auba.eiicon.net/projects/{id}/proposals/{n}` は取得できて
   「応募期間」「■応募締切：2026年10月18日（日）23:59まで」がそのまま読める
2. **一覧ページは 1 ページに何十件も並ぶ**（AUBA `/proposals`、Creww `/program_list`）。
   1 ページ 1 イベントの現在の抽出では扱えない。テーマの `site:` 検索で詳細ページを
   引かせる方針にした（一覧から複数イベントを取る仕組みは作らない）
3. **締切が「期間」で書かれる**。Creww は「募集期間：2026年 9月 14日～10月 4日」、
   AUBA は「応募期間 2026年9月9日〜2026年10月18日」。終わりが締切
4. **名指しの締切と期間が同じページに両方ある**。AUBA は期間を先に、締切を後に書く。
   先に見つけた方を採ると 23:59 の締切を落とすので、ラベルを確度で 3 段に分けた
   （名指しの締切 → 裸の「締切」→ 期間）
5. **ジャンルを名乗らないページがある**。AUBA の共創プログラムはタイトルが
   「【青森県2026】ヤマモト食品株式会社 - プログラム応募」で、本文にジャンルの語が
   1 度も出ない。一方で本文の隅には「ワークショップ」があり、本文全体で分類すると
   ワークショップ扱いになる
6. **実施日は書かれない**（AUBA の共創、Creww のアクセラとも）。段階1 の
   `eventStart` 任意化でそのまま載る。選考期間・面談期間は節目に入れた
7. Creww の詳細ページは JS で描くものがあり、取得しても本文が空になる場合がある。
   全件は拾えない

### 決めたこと

- テーマの `site:` は AUBA と Creww を指名する（`accelerator-*` は Creww を先に、
  `cocreation-*` は AUBA を先に）
- 締切ラベルを 3 段に分ける（上記 4）。`募集期間` / `応募期間` / `エントリー期間` と
  裸の `締切` を締切ラベルに足した
- ジャンルの判定は**タイトルだけ**を見る（`headline_kind`）。テーマのゲートは、
  タイトルが別ジャンルを名乗っているときだけ落とす。名乗っていないページは残す。
  本文全体での分類は表示用の `category` にとどめ、最初に現れた語を採る
- タイトルの「 | サイト名」除去は**見送った**。タイトルは `dedupKey` の材料なので、
  変えると既存のプールが二重になる。別途 dedup ごと考える

## 決めていないこと

- ~~`attributes` の値をどこまで抽出するか~~ → ラベル表で決定論的に拾う（段階2 で実装）。
  値は行のまま持ち、構造化も換算もしない。文章になっている行は捨てる
- タイトルの「 | サイト名」除去。`dedupKey` の材料なので、消すと既存プールが二重になる
- ジャンルをまたぐ推薦の重み（ビジコンとハッカソンを同じ土俵で並べるか、ジャンル別に見せるか）
- 主催者投稿のジャンル指定（投稿フォームに `kind` を足すか、本文から判定するか）

## 事前調査: ビジコンの告知ページを読む（2026-09-22 実施）

段階1 に着手する前に、実際のビジコン告知ページを**現在のパイプラインに通して**
何が取れて何が落ちるかを確かめた。Grounding 検索 3 本 → 取得 → 行ラベル抽出 /
引用抽出（ADR-007）の順に実データで確認した。

読んだページ: 日本政策金融公庫「高校生ビジネスプラン・グランプリ」（2 サイト）、
大阪市のビジネスプランコンテスト、学生向けビジコンのまとめ記事。

### 結果: 行ラベル抽出が効かないのは**ジャンル差ではない**

読んだ 4 ページで、行ラベル抽出（`find_labelled`）が取れたのは開催日 0 件・
締切 1 件（それもまとめ記事からの誤検出）だった。

ただしこれは**ビジコン特有の性質ではない**。同じ日に本番で回したハッカソンの収集
ログを見ると、**18 ページ中 行ラベルで取れたのは 2 件**で、残り 16 ページは引用抽出
（ADR-007）に回っている。実ページは告知の体裁が表組みで、`to_text` が見出しセルと
値セルを別々の行にするため、同一行を前提とする `find_labelled` はもともとほとんど
当たらない。ADR-007 で引用抽出を足したのは、まさにこの理由だった。

```
[20] 'エントリー締切'      ← ラベル行
     '2026年9月24日'       ← 値は次の行
[35] '締切'
     '2026年09月24日 (木)'
     'ビジネスプランシート応募・提出締切、必着'   ← 値の後にラベルが来ることもある
```

→ **抽出エンジンはこのままで足りる**。ビジコンだから新しい仕組みが要る、という
話ではない。

### 引用抽出（ADR-007）はビジコンでも正しく取れる

モデルに該当行を引用させると、同じページから次が取れた。

| 項目 | 引用 |
| --- | --- |
| タイトル | 第 14 回「創造力、無限大∞ 高校生ビジネスプラン・グランプリ」 |
| 開催（最終審査会） | 2027年1月10日 |
| 締切（エントリー） | 2026年9月24日 |
| 会場 | 場所: オンライン / 東京大学 伊藤謝恩ホール |
| 主催 | 日本政策金融公庫 |

パーサ側も、これらの引用（改行を含むものも）を正しく日付にできることを確認した。
「2026年11月下旬」「2026年10月~11月」は日が無いので `None`（推測しないので正しい）。

### 本当にジャンル差と言えること（2 点）

1. **「提出締切」の意味が反転する**。`NON_APPLICATION_DEADLINE_LABELS` の「提出締切」は
   ハッカソンの「作品提出締切」を除く目的で入っているが、ビジコンでは
   「ビジネスプランシート応募・提出締切、必着」が**本物の応募締切**である。
   同じ語で扱いが逆になるので、ラベル表を `kind` ごとに分ける必要がある
2. **実施日が無い／曖昧な告知がある**。ハッカソンは開催日が必ず書かれるが、
   ビジコンには「結果発表は 11 月下旬」だけで具体的な日が無いページがあった。
   `eventStart` 必須のままだと、締切だけ分かるビジコンを落とす

### ジャンルとは無関係に見つかった不具合（ついでに直す）

1. **「募集期間 A〜B締切」から開始日を締切として拾う**。
   「プラン募集期間 2026年6月4日(木)~2026年8月6日(木)13:00締切」に対し
   `find_application_deadline` は `parse_date`（= 6/4）を返す。採るべきは範囲の
   **終わり**（8/6）。ハッカソンでも同じ書き方があれば同様に誤る
2. **まとめ記事サイトの除外漏れ**。`startuplist.jp`（「学生向けビジコン13本」の記事）が
   `ARTICLE_HOSTS` に無いため候補になり、記事中の日付を締切として拾った。
   `compe.japandesign.ne.jp` はコンペ情報の集約サイトなので `aggregator` に分類すべき
   （現在は `official` 判定）

### 実装で追加で分かったこと（PR-1）

事前調査では見えていなかったが、実装して評価を回して初めて分かった検証側の壁が
2 つあった。どちらも「実施日があること」を暗黙の前提にしていた。

1. **必須根拠が `title` + `dates.eventStart` 固定**だった（`REQUIRED_EVIDENCE_FIELDS`）。
   実施日が無い告知は根拠不足として保留（quarantined）になる。→ 実施日が無いときは
   `title` + `dates.applicationDeadline` を必須根拠とする
2. **対象年の判定が実施日の年だけを見ていた**。ビジコンは「2026 年に応募 → 2027 年に
   最終審査」が普通なので、2026 年の機会が対象年違いで除外される。→ 締切か実施日の
   どちらかが対象年なら対象とみなす

検証ルールが変わったので `VALIDATION_RULE_VERSION` を 1.1.0 に上げ、§13.2 の評価を
再実行した（69 ケース、9/9 PASS）。

### 設計への影響（計画の修正）

抽出の仕組みは変えなくてよい。前倒しが要るのは上の「ジャンル差 2 点」に対応する分だけ。

1. **`eventStart` 必須の壁は段階1 で来る**。段階4（補助金）に置いた「`eventStart`
   任意化」を段階1 へ前倒しし、実施日なしの告知を締切だけで載せられるようにする
2. **ラベル表のジャンル別化も段階1**。「提出締切」の扱いが `kind` で逆になるため、
   後回しにできない
3. **`milestones` も段階1 が素直**。ビジコンの時系列は「エントリー開始 → 締切 →
   一次選考通過発表 → 最終審査会 → 結果発表」で、2 軸（締切 / 実施）に潰すと情報が
   落ちる。2 軸表示は「エントリー締切 → 最終審査会」に当て、残りを `milestones` に持つ
4. 年が複数あるページ（2025 / 2026 / 2027 が同一ページ）は普通だが、引用抽出は年入りの
   行を返すので実害はない

### 収集した語彙（ラベル表の素案、contest）

- 締切: エントリー締切、応募締切、募集締切、提出締切、応募・提出締切、必着、申込期限
- 開始: エントリー開始、募集開始、受付開始
- マイルストーン: 書類選考結果通知、一次選考通過（発表）、プレゼン審査、
  二次審査、最終審査会、結果発表、表彰式、集合研修
- 除外（contest では締切ではない）: 説明会、事前相談、質問受付

## 次の一手

事前調査は完了した（下記）。段階1 を次の順で進める。

> PR-1 は実装済み（`kind` / `eventStart` 任意化 / `milestones` / ラベル表のジャンル別化 /
> 範囲締切の end / 記事・集約サイトの分類 / 評価 8 ケース）。検証ルールを変えたので
> `VALIDATION_RULE_VERSION` を 1.1.0 に上げた。
>
> PR-2 も実装済み（ビジコン 3 テーマとジャンル別の検索クエリ、Job 7 タスク、
> `DAILY_GROUNDING_CAP` 60、一覧のジャンルチップ、詳細の時系列）。実装中に見つけた
> 2 件は同じ PR で直した: 実施日の無い投稿でフィードが落ちていた（`_displayable`)、
> 節目の名前が部分一致のラベルに縮んでいた（「一次審査結果発表」→「一次審査」）。
>
> PR-3 も実装済み（`SearchIntent.kinds`、語の表での種別判定、プールの種別絞り込み、
> チップの追従）。これで段階1は完了。
>
> 段階2 も実装済み。PR-1 が抽出とテーマ、PR-2 が `attributes` と「実施が近い順」。
> 事前調査は下記「事前調査: アクセラ・共創の告知ページを読む」に記録した。
> `attributes` の値はラベル表で決定論的に拾う（ADR-007 のとおりモデルには引用しか
> させない）。次は段階3（展示会）。

1. **PR-1 契約と抽出の土台**: `kind`、`eventStart` 任意化、`dates.milestones`、
   ラベル表のジャンル別化（`kind` で「提出締切」の扱いを変える）。
   ついでに 範囲締切の end 採用、`startuplist.jp` を記事ホストへ、コンペ情報サイトを
   `aggregator` へ。評価ケース `12-contest-*` を 8 件
2. **PR-2 テーマとUI**: `contest-kansai` / `-kanto` / `-online` を追加、
   `DAILY_GROUNDING_CAP` 40 → 60、ジャンルのチップ、実施日が無いイベントの 2 軸表示、
   詳細の時系列（マイルストーン）
3. **PR-3 プール探索の対応**: 解釈に `kinds` を足し、「ビジコン」「コンテスト」→ contest

1 は既存のハッカソン経路の挙動を変えうるので、評価（§13.2）を必ず通す。
